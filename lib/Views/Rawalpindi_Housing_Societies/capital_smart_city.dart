import 'package:flutter/material.dart';
class Capital_Smart_City extends StatelessWidget {
  const Capital_Smart_City({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
